# hermes-plugin

This package intentionally contains only lifecycle glue scaffolding.

It does **not** render widgets. Rendering remains owned by the TypeScript runtime core and sidecar so the Hermes patch surface stays shallow.
